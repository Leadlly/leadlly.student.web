import { TDashboardTodaysTopic } from "@/helpers/types";

const MobileUI = ({
  todaysTopics,
}: {
  todaysTopics: TDashboardTodaysTopic[];
}) => {
  return <div>MobileUI</div>;
};

export default MobileUI;
